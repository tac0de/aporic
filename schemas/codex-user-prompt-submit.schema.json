{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aporic.dev/schema/codex-user-prompt-submit-v1.json",
  "title": "Codex UserPromptSubmit hook input used by Aporic",
  "type": "object",
  "additionalProperties": true,
  "required": ["session_id", "hook_event_name", "cwd", "turn_id", "prompt"],
  "properties": {
    "session_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256,
      "description": "Runtime validation additionally enforces a 256 UTF-8 byte limit."
    },
    "hook_event_name": { "const": "UserPromptSubmit" },
    "cwd": { "type": "string", "minLength": 1 },
    "turn_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256,
      "description": "Runtime validation additionally enforces a 256 UTF-8 byte limit."
    },
    "prompt": { "type": "string", "minLength": 1 },
    "model": { "type": ["string", "null"] },
    "permission_mode": { "type": ["string", "null"] }
  }
}
