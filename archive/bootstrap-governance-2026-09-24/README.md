# Bootstrap governance archive

This directory preserves the active repository governance that existed before
the clean-room Aporic discussion on 2026-09-24.

Archived material:

- `repo/AGENTS.md`: prior rebuild workflow instructions.
- `repo/.aporic/`: prior local policy and configuration state.

The corresponding user-level snapshot is stored at:

`/Users/wonyoung_choi/.codex/archive/aporic-clean-slate-2026-09-24/global/`

That snapshot contains the former global `AGENTS.md` and the pre-change
`config.toml`. The source repositories and plugin caches were not deleted.
Archived material is evidence only and must not be reactivated implicitly.
