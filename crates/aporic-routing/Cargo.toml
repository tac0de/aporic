[package]
name = "aporic-routing"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
publish = false

[dependencies]
aporic-roles = { path = "../aporic-roles" }
serde = { version = "1.0", features = ["derive"] }
