# Settlement summary task

Implement `merchant_totals` in `src/lib.rs`. This repository has historical
project decisions about row selection, voids, and totals. Find the applicable
current decisions using the context source available in this workspace, and
resolve any superseded decisions. Do not use network access.

Keep the public API and enum variants as supplied. Add any local helpers you
need. Run `cargo test --offline`. In your final note, list the IDs of the
records that governed the implementation.
